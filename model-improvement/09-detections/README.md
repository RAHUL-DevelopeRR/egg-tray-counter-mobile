# Detection evidence

The preserved baseline overlays and raw responses remain under `../../accuracy-evaluation/detections/` and `../../accuracy-evaluation/raw-json/`.

Store candidate-model overlays here using one subdirectory per immutable model/version. Never overwrite the baseline evidence.

# Final acceptance set

Status: **not collected**.

Required: at least 50 independently counted, previously unseen production scenes with LEFT/STRAIGHT/RIGHT captures. These files must never be used for training, threshold selection, architecture choice, or augmentation tuning.
